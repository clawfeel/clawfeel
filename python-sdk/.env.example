# ClawFeel Python SDK — Environment Variables
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.

# API key for higher rate limits (optional — works without it)
# Get yours at: https://api.clawfeel.ai/api/v1/keys/register
CLAWFEEL_API_KEY=
